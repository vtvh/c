// Thuật toán
// - Bước 1: Nhập số nguyên N và số bit cần dịch offset
// - Bước 2: Dịch bit sang trái offset bit & dịch bit sang phải offset bit số nguyên N
// - Bước 3: Thông báo kết quả của số nguyên sau khi dịch bit và kết thúc thuật toán

#include <stdio.h>

int main()
{
  int n, offset;

  printf("n: ");
  scanf("%d",&n);
  printf("offset: ");
  scanf("%d",&offset);

}
